http:
  routers:
    keycloak-router:
      # Capture all Keycloak-specific paths
      rule: "Host(`syncup.local`) && (PathPrefix(`/realms`) || PathPrefix(`/resources`))"
      service: keycloak-service
      priority: 120
      entryPoints:
        - web

    # 1. Route for your .NET API
    api-router:
      rule: "Host(`syncup.local`) && (PathPrefix(`/api`) || PathPrefix(`/auth`))"
      service: api-service
      priority: 100
      entryPoints:
        - web

    # 2. Route for your React SPA (Catch-all)
    spa-router:
      rule: "Host(`syncup.local`)"
      service: spa-service
      priority: 10
      entryPoints:
        - web

  services:
    # This is where the actual URLs are defined!
    api-service:
      loadBalancer:
        servers:
          - url: "http://host.docker.internal:9001"

    spa-service:
      loadBalancer:
        servers:
          - url: "http://host.docker.internal:9000"

    keycloak-service:
      loadBalancer:
        servers:
          # Use the container name and the INTERNAL Keycloak port
          - url: "http://host.docker.internal:8080"